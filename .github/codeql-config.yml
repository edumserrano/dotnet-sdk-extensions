name: "Custom CodeQL config"

queries:
  - uses: security-and-quality

paths:
  - src
  - tests
paths-ignore: 
  # ignore auto generated files which otherwise have warnings about "Unnecessary using directive"
  - '**/*.AssemblyAttributes.cs'
  - '**/*.AssemblyInfo.cs'