{
  // for now the version can't be 7.0.x because I get errors on dotnet format.
  // perhaps these errors will stop on a future version but I've tested 7.0.302 and was still
  // getting errors.
  // perhaps these errors will also be gone when I stop supporting older TFMs.
  "sdk": {
      "version": "7.0.105",
      "rollForward": "disable",
      "allowPrerelease": false
  }
}
